# Goal completion history test plan

1. Add xUnit/Microsoft.NET.Test.Sdk to the existing shared test project.
2. Cover daily, weekly, monthly, and deactivation boundary semantics in `GoalOccurrenceCalculatorTests`.
3. Build and run the shared test project; review assertions against the occurrence implementation.
