# Goal completion history test research

The shared `KnowledgeTracker.Tests` project was an executable placeholder with no test framework. The bounded test target is the new UTC occurrence calculator, which is the deterministic foundation used by the period API and completion registration. Existing authentication fakes remain untouched.

Acceptance checklist: daily UTC boundaries; Monday-Sunday weekly boundaries; calendar-month boundaries; deactivation overlap; and no occurrence outside the active window.
